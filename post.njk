---
layout: base.njk
pagination:
  data: ghost.posts
  alias: post
  size: 1
permalink: /posts/{{ post.slug }}/
---
<h1>{{ post.title }}</h1>
<p>{{ post.published_at | displayDate }}</p>
<p>Reading time: {{ post.html | readingTime }}</p>
{{ post.html | safe }}
<h2>Tags:</h2>
<ul>
{% for tag in post.tags %}
  <li>
    <a href="/tag/{{ tag.slug }}/">{{ tag.name }}</a>
  </li>
{% endfor %}
</ul>